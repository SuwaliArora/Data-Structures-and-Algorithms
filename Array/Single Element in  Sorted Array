class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
       int res=0,n=nums.size();
        for(int i=0;i<n;i++){
            res^=nums[i];
        }
        return res;
    }
};
